# seCall 타임존 설정 — 렌더링 시간대 변환

> Phase: approval | Status: draft | Revision: 0
> Generated by tunaFlow

---

## Description

세션 마크다운 렌더링 시 모든 타임스탬프가 UTC로 출력되는 문제. `config.toml`에 `timezone` 설정을 추가하여 IANA 타임존(예: `Asia/Seoul`)으로 변환 렌더링. 내부 데이터(DB, 원본 JSON)는 UTC 유지, 렌더링 레이어에서만 변환.

## Expected Outcome

- `config.toml`에 `timezone = "Asia/Seoul"` 설정 시 vault MD의 모든 타임스탬프가 KST로 렌더링
- 기본값 UTC — 하위호환 유지
- vault 디렉토리 경로(`raw/sessions/YYYY-MM-DD/`)도 설정된 타임존 기준 날짜 사용

## Subtasks

### 1. 의존성 + Config 구조체

- **Status**: todo

#### 작업 지시

`chrono-tz` 추가, `Config`에 `timezone` 필드 추가, 파싱/검증 로직
- 변경: `Cargo.toml` (workspace), `crates/secall-core/Cargo.toml`, `config.rs`
- 리스크: chrono-tz 컴파일 시간 증가 (TZ 데이터베이스 임베딩)

### 2. 마크다운 렌더링 타임존 적용

- **Status**: todo

#### 작업 지시

`render_session()`의 6개 포맷 위치 + `session_vault_path()`에 타임존 변환 적용
- 변경: `markdown.rs` — frontmatter date/start_time/end_time, 세션 시간요약, 턴 타임스탬프, vault 경로 날짜
- 리스크: vault 경로 날짜가 바뀌면 기존 파일과 경로 불일치. `--force` 재수집으로 해결 가능

### 3. 보조 렌더링 위치 적용

- **Status**: todo

#### 작업 지시

index.rs, log.rs, chunker.rs, hooks/mod.rs, init.rs의 타임스탬프 포맷
- 변경: 5개 파일의 `format()` 호출에 타임존 변환 적용
- 리스크: init.rs는 `Utc::now()` 사용 — 현재시각도 변환 필요

### 4. 테스트 + 문서

- **Status**: todo

#### 작업 지시

타임존 변환 단위 테스트, README/CHANGELOG 업데이트
- 변경: `markdown.rs` 테스트, README 양쪽 Config 섹션 추가

## Revision History

- `2026-04-08 01:45` promoted (user) — Promoted from chat
- `2026-04-08 01:45` detail design requested (system) — Auto-requested on promotion
- `2026-04-08 01:51` subtask review started (user)
- `2026-04-08 01:51` subtask review completed (user)

